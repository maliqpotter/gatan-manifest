apiVersion: v1
kind: Service
metadata:
  name: my-postgresql-service
  labels:
    app: postgresql
spec:
  type: NodePort
  ports:
  - port: 5432
    targetPort: 5432
    nodePort: 32543
    name: postgres
  selector:
    app: postgresql
